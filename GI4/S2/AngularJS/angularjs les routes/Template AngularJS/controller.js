alert("working tst")
/*var myApp = angular.module('myApp', ['ngRoute']);
var users = [
    { id : 0, name : "Ahmed", age : 21},
    { id : 1, name : "Mina", age : 22},
    { id : 2, name : "Kamal", age : 30},
    { id : 3, name : "Fatima", age : 24},
    ];

/*myApp.controller('UserListCtrl', function ($scope) {
    $scope.users = users;
    alert("working");
});

myApp.config(function($routeProvider) {
    
            $routeProvider
            .when( '/users', {
                  templateUrl : 'users.html',
                  controller : 'UserListCtrl'
            }).when('/user/:id', {
                  templateUrl : 'user.html',
                  controller : 'UserCtrl'
            }).otherwise({
                  redirectTo : '/users'
            });
});*/

/*myApp.controller('UserCtrl',function($scope, $routeParams) {
if($routeParams.id){//Si id le parametre existe :
$scope.user = users[$routeParams.id];
}
});
*/